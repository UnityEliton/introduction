using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class F2C : MonoBehaviour
{
    public float f, c;
    // Start is called before the first frame update
    void Start()
    {
        f = (float)(9*c)/5  + 32;        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
